# SCOTTBERTRAND.COM — Environment Variables
# Copy to .env.local and fill in values

# ===========================================
# DATABASE (Vercel Postgres)
# ===========================================
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"
DIRECT_URL="postgresql://username:password@host:5432/database?sslmode=require"

# ===========================================
# AUTH.JS (NextAuth v5)
# ===========================================
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"
# Production URL (no trailing slash)
AUTH_URL="https://scottbertrand.com"

# ===========================================
# RESEND (Transactional Email)
# ===========================================
# Get from: https://resend.com/api-keys
# Auth.js v5 auto-detects AUTH_RESEND_KEY, but RESEND_API_KEY also works
AUTH_RESEND_KEY="re_xxxxxxxxxxxx"
# RESEND_API_KEY="re_xxxxxxxxxxxx"  # Alternative name (legacy)

# ===========================================
# VERCEL BLOB (File Storage)
# ===========================================
# Get from: Vercel Dashboard → Storage → Blob
BLOB_READ_WRITE_TOKEN="vercel_blob_xxxxxxxxxxxx"

# ===========================================
# FILE SECURITY
# ===========================================
# Generate with: openssl rand -base64 32
FILE_SIGNING_SECRET="your-file-signing-secret-here"

# ===========================================
# DATA ENCRYPTION (AES-256-GCM)
# ===========================================
# Required for encrypting sensitive lead/client data at rest
# Generate with: openssl rand -base64 32
# WARNING: Losing this key means losing access to encrypted data!
ENCRYPTION_KEY="your-encryption-key-here"

# ===========================================
# WEBHOOK SECURITY
# ===========================================
# Optional: Formspree webhook verification secret
# Set this after configuring webhook in Formspree dashboard
FORMSPREE_WEBHOOK_SECRET=""

# ===========================================
# RATE LIMITING (Upstash Redis)
# ===========================================
# Optional but recommended for distributed rate limiting
# Get from: https://upstash.com/
UPSTASH_REDIS_REST_URL="https://xxxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ===========================================
# NOTION (Field Notes Content)
# ===========================================
# Get from: https://www.notion.so/my-integrations
NOTION_API_KEY="ntn_xxxxxxxxxxxx"
NOTION_DATABASE_ID="your-database-id-here"

# ===========================================
# PUSHOVER (Notifications - Optional)
# ===========================================
PUSHOVER_USER_KEY="your-pushover-user-key"
PUSHOVER_API_TOKEN="your-pushover-api-token"

# ===========================================
# STRIPE (Payments)
# ===========================================
# Get from: https://dashboard.stripe.com/apikeys
# Use sk_test_* for development, sk_live_* for production
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxx"
# Webhook signing secret from Stripe Dashboard → Developers → Webhooks
# For local dev: use the secret from `stripe listen` CLI command
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"
# Optional: Publishable key (for client-side Stripe.js if needed)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxx"

# ===========================================
# VERCEL (Auto-populated by Vercel)
# ===========================================
# VERCEL_ENV="production" | "preview" | "development"
# VERCEL_URL="your-deployment-url.vercel.app"
